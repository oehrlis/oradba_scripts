# ------------------------------------------------------------------------------
# Configuration file for sync_from_peers.sh
# ------------------------------------------------------------------------------

# Optional: Override default list of peer hosts
PEER_HOSTS=("host1" "host2" "host3" "host4")

# Optional: Override default list of peer hosts
SSH_USER="oracle"
SSH_PORT="22"

# Optional: Add custom rsync options (base options will still be used)
# RSYNC_OPTS_EXTRA="--progress"

# Optional: Define a remote base path to override the default source path
# REMOTE_BASE="/u00/app/oracle/local/common"

# ------------------------------------------------------------------------------
# You can place this file in:
#  - ${SCRIPT_BASE}/etc/sync_from_peers.conf   (default location)
#  - ${ETC_BASE}/sync_from_peers.conf          (if ETC_BASE is set)
#  - or provide it via CLI using -c /path/to/conf
# ------------------------------------------------------------------------------
